#3. Program to find quotient and remainder of two numbers.

# Take values of dividend and divisor
a = int(input('Enter the dividend number:'))
b = int(input('Enter the divisor number:'))

# Perform Operation
quotient = a // b 
remainder = a % b

# Display result
print('Quotient = ',quotient)
print('Remainder = ',remainder)
